# Test report

Generated by `tools/make_test_report.py` on 2026-09-21 14:58 UTC (Python 3.12.10, Windows). Numbers come from a real pytest run.

**Result: 95 / 95 tests passed.**

| Test module | Passed | Failed | What it checks |
|---|---:|---:|---|
| `test_api` | 10 | 0 | FastAPI end to end on real captures: contract, declared labels, PFS unknown, error codes, size limit, samples, CORS |
| `test_contract` | 6 | 0 | Response contract, observed-vs-declared merge, validator catches bad responses |
| `test_docs` | 10 | 0 | Video script timing, sample reports are real PDFs, slide deck integrity |
| `test_frontend_samples` | 6 | 0 | Bundled frontend sample analyses match the contract and are regenerated from real API output |
| `test_ike_parser` | 15 | 0 | IKE parser on synthetic IKE_SA_INIT fixtures: selected vs offered proposal, NAT-T, unknown mode, truncated packet/file, ESP-in-UDP not mistaken for IKE, header validation |
| `test_orchestrate` | 6 | 0 | Testbed orchestration with a fake Docker: tcpdump before teardown, time-based capture, manifest upsert |
| `test_predict` | 15 | 0 | SHAP axis handling ((1,12,5) bug), predict_from_features return value, predict_from_pcap on real samples, timeline windows, banned features |
| `test_reports` | 8 | 0 | Real PDFs from real analyses, text content, HTML escaping, recommendations, /report endpoint |
| `test_scoring` | 8 | 0 | Scoring engine: strong/weak/unknown factors, unknown excluded (not weak), key-length handling, mode not defaulted |
| `test_testbed_configs` | 6 | 0 | Generated strongSwan configs: PFS DH group in esp=, GCM syntax, committed files match generator |
| `test_traffic_features` | 5 | 0 | Fixed 1 s windows, ESP-only filtering (decrypted duplicates and IKE excluded), no capture-length features |

## Other checks (run separately)

| Check | Command | Status when this report was written |
|---|---|---|
| Frontend lint | `cd frontend-developer && npm run lint` | passes (no warnings) |
| Frontend build | `npm run build` | passes (Vite chunk-size advisory only) |
| Real-browser end-to-end | `python tools/e2e_check.py` | passes: upload → results → chart → PDF download → bad-file error |
| Lean deploy environment | fresh venv from `requirements-backend.txt`, run API from a Docker-like folder layout | `/health`, `/analyze`, `/report` OK; ~320 MB RAM |

## Not covered / could not be run here

* Real Docker containers (strongSwan testbed, `docker compose up`) — Docker was not running; command order is tested with a fake Docker only.
* Deployment to Render/Railway/Vercel/Netlify — needs the owner's logins; configs are syntax-checked only.
* The IKE parser has never seen a *real* `IKE_SA_INIT` (none exists in the captured data); it is tested on synthetic fixtures built from RFC 7296.
