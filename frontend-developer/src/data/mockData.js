// Bundled sample analyses. These are NOT hand-written numbers: they are real responses from
// POST /analyze for two of the sample captures in data/samples/, generated by
// backend-developer/make_sample_responses.py and validated against the API contract in the tests.
// The app uses them (clearly labelled) when the backend is not reachable.
// weak_reference.json is different: a fixed reference CONFIGURATION (not a capture) for the
// "Compare against a weak setup" card, scored by the real scoring engine.
import strong from "./sample_strong.json";
import weak from "./sample_weak.json";
import weakReference from "./weak_reference.json";

export const bundledSamples = [
  { key: "strong", title: "Strong configuration (bundled)", analysis: strong },
  { key: "weak", title: "Weak configuration (bundled)", analysis: weak },
];

// Fixed reference configuration for the "Compare against a weak setup" card (see make_sample_responses.py).
export const weakExample = weakReference;

export const trafficClassColor = {
  web_browsing: "#4EA8DE",
  video_streaming: "#8B5CF6",
  voip: "#34D399",
  file_transfer: "#FBBF24",
  icmp: "#F87171",
  unrecognised: "#8B96AC",
};

export const trafficClassLabel = {
  web_browsing: "Web browsing",
  video_streaming: "Video streaming",
  voip: "VoIP",
  file_transfer: "File transfer",
  icmp: "ICMP (ping)",
  unrecognised: "Unrecognised traffic",
};
